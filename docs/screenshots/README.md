# Screenshot Notes

This folder stores the images used by the main README.

The current images are prototype screenshots from the Expo mobile demo. As the app changes, replace these files with fresh screenshots using the same filenames so the README stays current:

- `check-flow-preview.png`
- `investigation-preview.png`
- `profile-preview.png`
- `coach-preview.png`

Before a public launch, these should be replaced with clean App Store-style screenshots from a real simulator or device.
